"""Implement ingest(): return (items, truth). YOU export the ground truth — do not
spelunk a production DB. items: {anon_id: <whatever runner.run needs>}.
truth: {anon_id: "<value as string>"}. Keep data under data/ (gitignored)."""


def ingest():
    raise NotImplementedError("implement ingest for your project")
